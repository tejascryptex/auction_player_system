default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: auction_player_system_development

test:
  <<: *default
  database: auction_player_system_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
